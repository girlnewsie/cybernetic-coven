<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cybernetic Coven</title>
    <link rel="stylesheet" href={{ '/css/main.css' | url }}>
    <link rel="stylesheet" href={{ '/css/fonts.css' | url }}>
</head>
<body>
    <header class="top">
        <img src={{ '/img/white-logo.png' | url }}>
        <h1>Cybernetic Coven</h1>
        <h1>Cybernetic Coven</h1>
        <nav>
            <section>
                {% for label,link in map %}
                <a href={{link | url}}>{{label}}</a>
                {% endfor %}
            </section>
            <small>Support us on:</small>

            <section>
                {% for label,link in links %}
                <a href={{link | url}}>{{label}}</a>
                {% endfor %}
            </section>
        </nav>
    </header>
    <main>
        <article>
            <h1>{{ title }}</h1>
            {{ content | safe }}
            <footer>
                <nav>
                    <small>Support us on:</small>
                    <section>
                        {% for label,link in links %}
                        <a href={{link | url}}>{{label}}</a>
                        {% endfor %}
                    </section>
                </nav>
                <small>Cybernetic Coven ©2023</small>

            </footer>
        </article>
    </main>
    <img class="watermark" src={{ '/img/white-logo.png' | url }}>

</body>
</html>